@model RetoTecnicoAjinomoto.ViewModels.TareasViewModelRegister


@{
    ViewBag.Title = "Tareas";
}



<h2>Listado de Tareas</h2>
@*<h4>Filtro por Estado</h4>
<div class="form-group">
    Estado  @Html.DropDownListFor(model => model.Tarea.IdEstadoTarea,
new SelectList(Model?.ListaEstadoTareas, "Id", "Nombre"), "Seleccione una categoría")
    @Html.ActionLink("Buscar", "Search", "Tareas", new { id = Model.Tarea?.IdEstadoTarea }, new { @class = "btn btn-info" })
</div>*@


<html>
<head>

</head>
<body>
    <p>
        @Html.ActionLink("Crear", "Create", "Tareas", "", new { @class = "btn btn-success" })
    </p>
    <table class="table">
        <tr>
            <th>
                Titulo
            </th>
            <th>
                Descripción
            </th>
            <th>
                Estado
            </th>
            <th></th>
        </tr>

        @foreach (var item in ViewBag.ItemsTarea)
        {
            <tr>
                <td>
                    @item.Titulo
                </td>
                <td>
                    @item.Descripcion
                </td>
                <td>
                    @item.DescripcionEstado
                </td>

                <td>
                    @Html.ActionLink("Editar", "Edit", "Tareas", new { id = item.Id }, new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Eliminar", "Delete", "Tareas", new { id = item.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
</body>
</html>

